<form method="post" action="{{ path('app_child_presence_crud_delete', {'id': child_presence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child_presence.id) }}">
    <button class="btn">Delete</button>
</form>
