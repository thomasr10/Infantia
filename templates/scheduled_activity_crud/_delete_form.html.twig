<form method="post" action="{{ path('app_scheduled_activity_crud_delete', {'id': scheduled_activity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scheduled_activity.id) }}">
    <button class="btn">Supprimer</button>
</form>
